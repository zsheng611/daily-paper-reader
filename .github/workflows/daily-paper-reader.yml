name: daily-paper-reader

on:
  schedule:
    # - cron: "30 18 * * *" # 北京时间次日 02:30（UTC 18:30）
    - cron: "0 8 * * *"
      timezone: "America/Edmonton"
  workflow_dispatch:
    inputs:
      run_enrich:
        description: "Run 0.enrich_config_queries.py（默认关闭，按需开启）"
        required: false
        default: "false"
      fetch_days:
        description: "回溯1–365天；31天以上进行arXiv专题评审，结果在原Sidebar阅读（DeepSeek按实际用量计费）"
        required: false
        default: "10"
      fetch_mode:
        description: "1–30天：auto/standard/skims；31–365天固定为arXiv专题回溯"
        required: false
        default: "auto"
      profile_tag:
        description: "仅运行指定词条 tag；留空表示全量运行"
        required: false
        default: ""
      reranker_profile:
        description: "Reranker：默认 public-zwwen-rerank；可改 siliconflow-qwen3-0.6b（仅云端）"
        required: false
        default: "public-zwwen-rerank"

permissions:
  contents: write

concurrency:
  group: daily-paper-reader
  cancel-in-progress: false

jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    outputs:
      research_days: ${{ steps.window.outputs.days }}
    env:
      DPR_MODELS_REMOTE_ONLY: "1"
      DPR_EMBED_ALLOW_LOCAL_FALLBACK: "0"
      DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
      DEEPSEEK_BASE_URL: ${{ secrets.DEEPSEEK_BASE_URL }}
      DEEPSEEK_MODEL: ${{ secrets.DEEPSEEK_MODEL }}
      RERANK_PROFILE: ${{ github.event.inputs.reranker_profile || secrets.RERANK_PROFILE || 'public-zwwen-rerank' }}
      RERANK_PROVIDER: ${{ secrets.RERANK_PROVIDER }}
      RERANK_MODEL: ${{ secrets.RERANK_MODEL }}
      RERANK_API_KEY: ${{ secrets.RERANK_API_KEY }}
      RERANK_API_BASE_URL: ${{ secrets.RERANK_API_BASE_URL }}
      PUBLIC_RERANK_API_KEY: ${{ secrets.PUBLIC_RERANK_API_KEY }}
      PUBLIC_RERANK_API_BASE_URL: ${{ secrets.PUBLIC_RERANK_API_BASE_URL || 'https://zwwen.online/rerank' }}
      SILICONFLOW_API_KEY: ${{ secrets.SILICONFLOW_API_KEY }}
      SILICONFLOW_RERANK_URL: ${{ secrets.SILICONFLOW_RERANK_URL }}
      SILICONFLOW_RERANK_MIN_INTERVAL_SECONDS: ${{ secrets.SILICONFLOW_RERANK_MIN_INTERVAL_SECONDS || '8' }}
      MKL_THREADING_LAYER: GNU
      DPR_RERANK_GLOBAL_POOL_LIMIT: "120"
      DPR_RERANK_GUARANTEED_PER_LANE: "2"
      DPR_EMBED_API_TIMEOUT: "60"
      DPR_EMBED_API_URL: ${{ secrets.DPR_EMBED_API_URL || 'https://zwwen.online/embed' }}
      DPR_EMBED_API_KEY: ${{ secrets.DPR_EMBED_API_KEY }}
      LLM_PRIMARY_BASE_URL: ${{ secrets.LLM_PRIMARY_BASE_URL }}
      SUMMARY_API_KEY: ${{ secrets.SUMMARY_API_KEY }}
      SUMMARY_BASE_URL: ${{ secrets.SUMMARY_BASE_URL }}
      SUMMARY_MODEL: ${{ secrets.SUMMARY_MODEL }}
      PYTHONUNBUFFERED: "1"

    steps:
      - name: Random delay to spread scheduled runs
        if: github.event_name == 'schedule'
        run: |
          DELAY=$(shuf -i 0-3599 -n 1)
          echo "Sleeping ${DELAY}s (≈$((DELAY/60))min) to stagger fork runs over 1 hour …"
          sleep $DELAY

      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Validate requested window
        id: window
        env:
          REQUESTED_DAYS: ${{ github.event.inputs.fetch_days }}
        run: |
          python - <<'PY'
          import os
          raw = os.environ.get('REQUESTED_DAYS', '').strip()
          days = int(raw) if raw else 10
          if not 1 <= days <= 365:
              raise SystemExit('fetch_days 必须在 1–365 天之间')
          with open(os.environ['GITHUB_OUTPUT'], 'a') as output:
              output.write(f'long_range={str(days > 30).lower()}\n')
              output.write(f'days={days}\n')
          PY

      - name: Cache Python deps
        uses: actions/cache@v5
        with:
          path: |
            ~/.cache/pip
            ~/.cache/uv
          key: ${{ runner.os }}-dpr-embed-deps-v1-${{ hashFiles('requirements.txt') }}

      - name: Install deps (skip sqlite3)
        run: |
          python - <<'PY'
          import re
          lines = open("requirements.txt", "r", encoding="utf-8").read().splitlines()
          lines = [l for l in lines if l.strip() and not re.match(r"^sqlite3\\b", l)]
          open("/tmp/req.txt", "w", encoding="utf-8").write("\n".join(lines))
          PY
          python -m pip install --upgrade pip
          python -m pip install uv
          uv pip install --system -r /tmp/req.txt

      - name: Check cloud embedding and reranker
        run: python scripts/check_cloud_models.py --require-lightweight

      - name: Restore long-range review progress
        uses: actions/cache/restore@v5
        with:
          path: |
            .local-runs/long-range-cache
            .local-runs/starter-pack-cache
            .local-runs/topic-research-rerank-cache
          key: long-range-v1-${{ github.ref_name }}-${{ github.run_id }}-${{ github.run_attempt }}
          restore-keys: |
            long-range-v1-${{ github.ref_name }}-

      - name: Run Retrieval and Embedding
        # 留出保存评审缓存的时间；失败后重跑可复用仍在缓存中的已完成评分。
        timeout-minutes: 100
        env:
          HF_HOME: ${{ github.workspace }}/hf_cache
          HUGGINGFACE_HUB_CACHE: ${{ github.workspace }}/hf_cache
          HF_HUB_DISABLE_SYMLINKS: "1"
        run: |
          echo "Running main script..."
          RUN_ENRICH="${{ github.event.inputs.run_enrich }}"
          FETCH_DAYS="${{ github.event.inputs.fetch_days }}"
          FETCH_MODE="${{ github.event.inputs.fetch_mode }}"
          PROFILE_TAG="${{ github.event.inputs.profile_tag }}"
          RERANKER_PROFILE_INPUT="${{ github.event.inputs.reranker_profile }}"
          if [ -n "$RERANKER_PROFILE_INPUT" ]; then
            export RERANK_PROFILE="$RERANKER_PROFILE_INPUT"
          fi
          ARGS=()
          if [ "$RUN_ENRICH" = "true" ]; then
            ARGS+=(--run-enrich)
          fi
          if [ -n "$FETCH_DAYS" ]; then
            ARGS+=(--fetch-days "$FETCH_DAYS")
          fi
          if [ -n "$FETCH_MODE" ]; then
            ARGS+=(--fetch-mode "$FETCH_MODE")
          fi
          if [ -n "$PROFILE_TAG" ]; then
            ARGS+=(--profile-tag "$PROFILE_TAG")
          fi
          python src/main.py "${ARGS[@]}" --embedding-device cpu --embedding-batch-size 8

          # 让前端可以在不配置 GitHub Token 的情况下直接读取配置（只读快照）
          cp -f config.yaml docs/config.yaml


      - name: Save long-range review progress
        if: always() && (hashFiles('.local-runs/long-range-cache/**') != '' || hashFiles('.local-runs/starter-pack-cache/**') != '')
        uses: actions/cache/save@v5
        with:
          path: |
            .local-runs/long-range-cache
            .local-runs/starter-pack-cache
            .local-runs/topic-research-rerank-cache
          key: long-range-v1-${{ github.ref_name }}-${{ github.run_id }}-${{ github.run_attempt }}

      - name: Upload long-range report
        if: success() && steps.window.outputs.long_range == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: arxiv-long-range-results
          include-hidden-files: true
          path: |
            docs/long-range/
            docs/starter-pack/
            docs/*-*/
            .local-runs/starter-pack-cache/runs/
          retention-days: 7
          if-no-files-found: error

      - name: Cleanup non-recommend archives
        if: steps.window.outputs.long_range != 'true'
        run: |
          if [ -d archive ]; then
            find archive -mindepth 2 -maxdepth 2 -type d \( -name raw -o -name filtered -o -name rank \) -exec rm -rf {} +
          fi

      - name: Commit results
        if: github.repository != 'ziwenhahaha/daily-paper-reader'
        env:
          REQUESTED_DAYS: ${{ steps.window.outputs.days }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          branch="${GITHUB_REF_NAME:-main}"

          # 新90/365任务只发布结果，不改日常配置、不自动覆盖远端冲突。
          if [ "$REQUESTED_DAYS" = "90" ] || [ "$REQUESTED_DAYS" = "365" ]; then
            if [ "$GITHUB_REPOSITORY" = 'ziwenhahaha/daily-paper-reader' ]; then exit 0; fi
            git add docs
            if git diff --cached --quiet; then exit 0; fi
            git commit -m '[update] 更新专题回溯结果' -m 'Co-Authored-By: xixi <3495302215@qq.com>'
            git fetch origin "$branch"
            git rebase "origin/$branch"
            git push origin HEAD:"$branch"
            exit 0
          fi

          shopt -s nullglob
          paths=(docs config.yaml)
          if [ -f archive/arxiv_seen.json ]; then
            paths+=(archive/arxiv_seen.json)
          fi
          if [ -f archive/crawl_state.json ]; then
            paths+=(archive/crawl_state.json)
          fi
          if [ -f archive/carryover.json ]; then
            paths+=(archive/carryover.json)
          fi
          for d in archive/*/recommend; do
            paths+=("$d")
          done

          echo '{"owner":"'"$GITHUB_REPOSITORY_OWNER"'","repo":"'"${GITHUB_REPOSITORY#*/}"'"}' > docs/.repo-owner.json

          git add "${paths[@]}"
          if [ -n "$(git ls-files -- archive)" ]; then
            git add -u -- archive
          fi
          if git diff --cached --quiet; then
            echo "No changes to commit."
            exit 0
          fi

          git commit -m "[chore] daily pipeline"

          git status --short
          git diff --quiet
          git diff --cached --quiet
          git fetch origin "$branch"
          git rebase -X theirs "origin/$branch"
          if [ "${{ steps.window.outputs.long_range }}" = "true" ]; then
            # 排队任务的checkout可能早于前一次结果提交；合并后重扫所有报告。
            python src/long_range_review.py --rebuild-index
            git add docs/long-range/index.json docs/long-range/README.md docs/_sidebar.md docs/*-*/
            if ! git diff --cached --quiet; then
              git commit -m "[chore] rebuild long-range navigation"
            fi
          fi
          git push origin HEAD:"$branch"

  verify-topic-pages:
    needs: run
    if: github.repository != 'ziwenhahaha/daily-paper-reader' && (needs.run.outputs.research_days == '90' || needs.run.outputs.research_days == '365')
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
      pages: write
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ github.ref_name }}
      - uses: actions/setup-python@v6
        with:
          python-version: '3.11'
      - run: python -m pip install requests
      - name: Verify existing Pages publication
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: python scripts/request_pages_build.py --timeout 600
